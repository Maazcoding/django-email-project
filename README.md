A simple Django project with Home, About, and Contact pages. The contact form sends emails to the admin (prints to console in development).

config : is projectname
pages : is appname

├── config/          # Project settings & URLs
├── pages/           # App (views, forms, templates, static)
├── manage.py
├── requirements.txt
└── README.md
